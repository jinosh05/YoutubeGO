PyQt5
yt-dlp

# ffmpeg must be installed on the system.
# For Linux: sudo apt install ffmpeg or sudo pacman -S ffmpeg
# For macOS: brew install ffmpeg
# For Windows: Download from https://ffmpeg.org/download.html and add to system PATH.
